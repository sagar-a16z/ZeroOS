[package]
name = "zeroos-runtime-musl"
version.workspace = true
edition.workspace = true
description = "Musl libc compatibility layer for ZeroOS"

[lib]
name = "zeroos_runtime_musl"
path = "src/lib.rs"
crate-type = ["rlib"]

[dependencies]
debug.workspace = true
foundation.workspace = true

[features]
default = []
debug = ["debug/debug"]
bounds-checks = []
# Enable bootstrap code (_init, _fini, __runtime_bootstrap)
# Platforms that provide their own boot code should NOT enable this feature
bootstrap = []
